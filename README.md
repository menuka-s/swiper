# Swiper

Swiper is an iOS app written in Swift. 

The purpose of this app is to show just one image at a time. This app prevents a user from swiping through the owner's phone gallery. Instead, this app will take a picture of the current user. 